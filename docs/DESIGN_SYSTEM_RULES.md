# RuTren: Система Дизайна и Правила UI/UX

> **Статус**: УТВЕРЖДЕНО
> **Версия**: 1.0
> **Контекст**: Глобальные правила дизайна для приложения RuTren.
> **Философия**: Неоновый Глассморфизм (Neon Glassmorphism) • Только темная тема • Премиальный вид

---

## 1. 🎨 Цветовая палитра (Токены)

Все цвета должны использоваться через CSS-переменные или утилиты Tailwind.

### Основные цвета

| Токен | Значение | Класс Tailwind | Использование |
|-------|----------|----------------|---------------|
| **Акцентный Зеленый** | `#9DFF00` | `text-[#9DFF00]`, `bg-[#9DFF00]` | Основные кнопки, активные состояния, подсветка, активные иконки |
| **Красный (Опасность)** | `#FF6060` | `text-[#FF6060]`, `bg-[#FF6060]` | Ошибки, удаление, кнопки "стоп" |
| **Темный Фон** | `#000000` | `bg-black` | Основной фон приложения |

### Глассморфизм и Фоны

| Токен | Значение / Логика | CSS Реализация | Использование |
|-------|-------------------|----------------|---------------|
| **Стекло (База)** | `FFFFFF` / 10% | `bg-white/10 backdrop-blur-md` | Фон карточек, основные контейнеры |
| **Слой Стекла** | `#535353` / 40% | `bg-[#535353]/40` | Элементы *внутри* Базы Стекла (вложенные) |
| **Оверлей** | `000000` / 60% | `bg-black/60` | Модальные окна, затемнение |

### Цвета Текста

| Токен | Значение | Класс Tailwind | Использование |
|-------|----------|----------------|---------------|
| **Текст Основной** | `#FFFFFF` | `text-white` | Заголовки, основной контент |
| **Текст Вторичный** | `FFFFFF` / 60% | `text-white/60` | Подзаголовки, подписи, описания |

### Границы и Разделители

| Токен | Значение | Использование |
|-------|----------|---------------|
| **Граница (Неактив)** | `#7C7C7C` (1px) | Неактивные инпуты, разделители, границы карточек (по умолчанию) |
| **Граница (Актив)** | `#9DFF00` (1px) | Активные инпуты, фокус, выбранные элементы |

---

## 2. ✍️ Типографика

**Шрифт**: `Poppins` (Веса: 400, 500, 600, 700)

| Стиль | Размер | Вес | Line Height | Tracking | Использование |
|-------|--------|-----|-------------|----------|---------------|
| **H1 (Крупный)** | `26px` | 700 (Bold) | `1.2` | `-0.02em` | Заголовки экранов, Главные итоги (напр. "Тоннаж") |
| **H2 (Обычный)**| `20px` | 600 (Semi) | `1.3` | `-0.01em` | Заголовки секций, Названия карточек |
| **Body (База)** | `16px` | 400/500 | `1.5` | `normal` | Основной текст, инпуты, списки |
| **Small** | `13-14px`| 400 | `1.4` | `normal` | Подписи, вторичная инфо |

---

## 3. 📐 Макет и Отступы

- **Отступы Экрана**: `20px` (горизонтальные) для всех экранов. Контент не должен прилипать к краям.
- **Радиус Скругления**:
  - **Карточки**: `20px` (Стандарт), `24px` (Крупные контейнеры).
  - **Кнопки**: `12px` или `Full Pill` (Полностью круглые).
  - **Инпуты**: `16px`.
- **Безопасная Зона (Safe Area)**: Нижняя навигация и верхний статус-бар должны учитывать отступы (padding-bottom/top).

---

## 4. 🧩 База Компонентов

### A. Кнопки (Buttons)

1. **Primary (Основная)**:
    - Фон: `#9DFF00` (Акцентный Зеленый).
    - Текст: Черный (`#000000`) или Темно-серый 700. Вес: 600.
    - Анимация: `active:scale-95`.
    - Тень: Мягкое свечение `shadow-[0_0_20px_rgba(157,255,0,0.3)]`.
2. **Secondary / Ghost (Вторичная)**:
    - Фон: Прозрачный.
    - Граница: `#9DFF00` (1px).
    - Текст: `#9DFF00`.
3. **Icon Button (Кнопка-иконка)**:
    - Размер: 40x40px или 48x48px.
    - Фон: База Стекла.

### B. Поля Ввода (Inputs - Glass)

- **Покой (Idle)**: Фон `bg-white/5` Граница `#7C7C7C`. Текст `white`.
- **Фокус (Focus)**: Фон `bg-white/10` Граница `#9DFF00`. Эффект свечения.
- **Плейсхолдер**: Текст `white/30`.

### C. Карточки (Паттерн "Glass")

- **Структура**:

    ```css
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(16px); 
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    ```

- **Вложенные Элементы**: Если внутри карточки есть блок (например, строка подхода), используйте **Слой Стекла** (`#535353 / 40%`) для создания глубины.

### D. Навигация

- **Нижний Таббар**: Плавающая стеклянная пилюля или док-бар.
  - Активный Таб: Иконка `#9DFF00`, возможно светящаяся точка.
  - Неактивный: Иконка `white/60`.

---

## 5. ✨ Анимации и Взаимодействие (UX)

### Принципы

1. **Живой интерфейс**: Ничего не появляется мгновенно. Используем fade и slide.
2. **Интерактивность**: Все кликабельные элементы (карточки, кнопки) должны иметь реакцию `active:scale` (0.95 или 0.98).
3. **Живые Графики**: Графики должны рисоваться/анимироваться при загрузке (`recharts` с анимацией).

### Микро-анимации

- **Клик**: `scale: 0.95` переход `0.1s ease-out`.
- **Переключение (Toggle)**: Плавный сдвиг с интерполяцией цвета (Серый -> Зеленый).
- **Drag & Drop**:
  - Используется для: Сортировки упражнений, привычек.
  - Отклик: Элемент приподнимается (тень растет, scale 1.05) и прилипает к месту.

### Переходы (Transitions)

- **Смена Страниц**: Сдвиг (Slide) или Затухание через черный (Fade through black).
- **Модалки**: Выезд снизу (Sheet) с затемнением фона.

---

## 6. 📊 Графики и Данные

- **Библиотека**: Recharts (Рекомендуется) или VisX.
- **Стиль**: Тонкие линии (2px), цвет `#9DFF00`. Градиентная заливка под линией (от 20% до 0% прозрачности).
- **Тултип**: Кастомный стеклянный тултип с конкретными значениями.

---

## ❓ Вопросы / Уточнения

1. **Размытие (Blur)**: Ты упоминал `background-blur -4`. Ты имел в виду `4px` (blur-sm) или специфичный хак? Обычно размытие — это положительное число (10-20px для стекла).
2. **Drag & Drop**: Нужен везде (списки, привычки, подходы) или только на конкретных экранах?
3. **Иконки**: Подтверждаем использование стандартного набора **Lucide-React**?
